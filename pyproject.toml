[project]
name = "mailchimp-reports-worker"
version = "0.1.0"
description = "Event-driven Mailchimp campaign reporting worker"
readme = "setup.md"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
